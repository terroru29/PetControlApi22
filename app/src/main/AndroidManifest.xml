<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:overrideLibrary="com.android.car.ui">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_petcontrol"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_petcontrol_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetControlApi22"
        tools:targetApi="31">
        <activity
            android:name=".FormPetsPetControl"
            android:exported="true" />
        <activity
            android:name=".FastConfigurationPetControl"
            android:exported="true" />
        <activity
            android:name=".AdapterPetsPetControl"
            android:exported="true" />
        <activity
            android:name=".AddPetPetControl"
            android:exported="true" />
        <activity
            android:name=".RegisterPetControl"
            android:exported="true" />
        <activity
            android:name=".LoginPetControl"
            android:exported="true" />
        <activity
            android:name=".InicioPetControl"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuInferiorPetControl"
            android:exported="true">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>